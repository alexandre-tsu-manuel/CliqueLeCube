#ifndef HPP_MENU_RECTANGLE
    #define HPP_MENU_RECTANGLE

    Uint32 moveRectangle(Uint32 interval, void* nothing);
    SDL_Rect trajectoireCirculaire(double rayon, double angle);
    void testTrajectoire();

#endif
